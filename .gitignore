# Rust / Tauri
target/
src-tauri/target/
src-tauri/gen/

# Frontend
node_modules/
dist/
.vite/

# Env / local
.env
.env.*
!.env.example
*.local

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# Downloaded CI artifacts (`just ci-artifacts`)
artifacts/

# Signing material — certificates and keys never belong in the repository.
*.p12
*.pfx
*.cer
*.pem
*.key
*.mobileprovision
*.keystore

# The `ys` sidecar, staged by `scripts/sidecar.mjs` before bundling.
src-tauri/binaries/
