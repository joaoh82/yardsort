OpenCode 1.18.31, recorded 2026-09-25 with scripts/record-opencode.sh.
One 'opencode run' turn under a throwaway XDG_DATA_HOME, the capture plugin given through OPENCODE_CONFIG_CONTENT.
Each file is one plugin hook call or bus event ({hook, payload, directory}); streaming deltas, text and reasoning parts, and catalogue events are left out.
Paths and ids are redacted; 01-init.json shows the inherited YARDSORT_* variables.
